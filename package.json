{
  "name": "hipst",
  "version": "0.0.4",
  "description": "Full-stack framework",
  "main": "dist/index.js",
  "module": "dist/index.js",
  "types": "dist/index.d.ts",
  "type": "module",
  "author": "mito11",
  "license": "MIT",
  "keywords": [
    "fullstack",
    "framework"
  ],
  "files": [
    "dist"
  ],
  "bin": {
    "hipst": "dist/src/cli/hipst.js"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5.9.2"
  },
  "scripts": {
    "start": "bun run index.ts",
    "build": "tsc -p tsconfig.build.json && bun run scripts/postbuild.ts",
    "prepublishOnly": "bun run build",
    "hipst": "bun run src/cli/hipst.ts"
  },
  "dependencies": {
    "csstype": "^3.1.3"
  },
  "peerDependencies": {
    "bun-types": "^1.2.22"
  },
  "engines": {
    "bun": ">=1.2.0"
  }
}
